import { FC } from "react";
import { Button } from "@mantine/core";

const App: FC = () => {
    return (
        <div>
            <Button variant="filled">Tests</Button>
        </div>
    );
};

export default App;
