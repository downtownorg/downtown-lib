import { FC } from "react";
import { Button } from "@mantine/core";

const App: FC = () => {
    return (
        <div>
            <Button variant="filled">Testing</Button>
        </div>
    );
};

export default App;
